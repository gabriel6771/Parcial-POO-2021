package Parcial.DominioParcial;

import java.util.ArrayList;
import java.util.List;

public class CarritoDeMercado {
    public static final byte CAPACIDAD_MAX=20;
    private String nombre;
    private String productos;
    private List<Producto>;

    public String agregar(Producto producto) {
        if (CarritoDeMercado < CAPACIDAD_MAX) {
                this.productos.add(producto);
                return true;
            } else {
                return false;
            }
        } else {
            return false;


    public Producto buscar (String nombre){
        Producto productobuscado = null;{

            for (Producto producto1: this.producto) {
                if (Producto.getNombreProducto() == producto) {
                    productobuscado = producto;
                    break;
                }
            }

        }


    public buscar(TipoProducto)

        public Producto buscar(String tipo) {
            Producto tipoBuscado = null;

            for (Producto tipo: this.tipo) {
                if (tipo.getTipo(ArrayList<tipo>);
                tipoBuscado = tipo;
                break;
            }
        }

        return tipoBuscado;
    }

     public SacarProducto (String nombre){
                if () {
                    this.productos.substring(nombre);
                    return true;
                } else {
                    return false;
                }
        }

     }

     public int calcularTotal;




     public Producto productoMasCaro;







    }





